language: node_js
node_js: stable

# S: Build Lifecycle
install:
  - npm install


#before_script:
 # - npm install -g gulp

script:
  - hexo g

after_script:
  - cd ./public
  - git init
  - git config user.name "SunshineABC"
  - git config user.email "dealloc@sina.cn"
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://${token}@${GH_REF}" master:master
# E: Build LifeCycle

branches:
  only:
    - blog_resource
env:
 global:
   - GH_REF: github.com/SunshineABC/sunshineABC.github.io.git
