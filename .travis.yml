language: node_js
node_js: stable

# S: Build Lifecycle
install:
  - npm install


#before_script:
 # - npm install -g gulp

script:
  - hexo g

after_script:
  - cd ./public
  - git init
  - git config user.name "SunshineABC"
  - git config user.email "dealloc@sian.cn"
  - git add -A
  - git commit -m "Update docs"
  - git push --force --quiet "https://${4af729da52dbf88472eb4713c4b461cf5e18e0}@${GH_REF}" master:master
# E: Build LifeCycle

branches:
  only:
    - dev
env:
 global:
   - GH_REF: github.com/SunshineABC/sunshineABC.github.io.git
